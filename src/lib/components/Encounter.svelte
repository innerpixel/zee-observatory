<script lang="ts">import type { Snippet } from 'svelte';import type { Chapter } from '$lib/types/chapter';let { chapter,children }:{chapter:Chapter;children:Snippet}=$props();</script>
<div class="chapter-line"><span>CHAPTER {chapter.chapterNumber}{chapter.totalChapters?` OF ${chapter.totalChapters}`:''}</span><i></i><span>{chapter.title.toUpperCase()}</span></div>
<div class="encounter"><div class="story"><figure><img src={chapter.encounter.image} alt={chapter.encounter.imageAlt}><figcaption>{chapter.encounter.imageCaption}</figcaption></figure><p class="eyebrow">{chapter.encounter.timestamp} · {chapter.encounter.setting}</p><h1>{chapter.encounter.title}</h1>{#each chapter.encounter.blocks as block}{#if block.type==='quote'}<blockquote>“{block.text}”</blockquote>{:else}<p class:weather={block.type==='field-weather'}>{block.text}</p>{/if}{/each}<div class="zee"><b>Z</b><p><strong>Zee asks:</strong> “{chapter.encounter.zeeQuestion}”</p></div></div>{@render children()}</div>
