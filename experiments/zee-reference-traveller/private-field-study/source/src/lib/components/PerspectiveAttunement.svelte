<script lang="ts">import type { Perspective } from '$lib/types/chapter';let { perspectives }:{perspectives:Perspective[]}=$props();let selectedId=$state('');let selected=$derived(perspectives.find(item=>item.id===selectedId)??perspectives[0]);</script>
<section class="attunement"><header><div><b>◉ PERSPECTIVE ATTUNEMENT</b><small>An instrument from Zee’s world</small></div><span>● FIELD AVAILABLE</span></header><div class="attune-body"><div class="perspectives" role="tablist" aria-label="Choose a perspective">{#each perspectives as item}<button role="tab" aria-selected={selected.id===item.id} onclick={()=>selectedId=item.id}><span>{item.index} · {item.kind}</span>{item.buttonLabel}</button>{/each}</div><div class="weather-panel" role="tabpanel" aria-live="polite"><small>{selected.fieldLabel}</small><h3>{selected.title}</h3><p>{selected.copy}</p><footer>{selected.note}</footer></div></div></section>
